function initialData = generateInitialDataStruct(arg)
% Generate a struct with initial data for the different solvers.
%
% The returned struct consists of the following members:
%   kind: which kind of format is used for the initial data @type string
%
% Parameters:
%
% Return values:
%
% @todo Not yet implemented!

  initialData = {};
  initialData.kind = 'empty';
end
